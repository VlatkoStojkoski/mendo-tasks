#include <bits/stdc++.h>

using namespace std;

vector<int> S;

bool f(int a, int k)
{
	int
}

int main()
{
	int n, k;
	cin >> n >> k;

	S.resize(n);

	for (int i = 0; i < n; i++)
		cin >> S[i];

	sort(S.begin(), S.end());

	for (int i = 0; i < n; i++)
	{
	}

	return 0;
}